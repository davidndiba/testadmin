import { defineConfig } from '@umijs/max';

export default defineConfig({
  define: {
    // TODO: add the real base url for planner here
    API: 'https://planner-dev-backend.microvision.co.ke/api/v1',
  },
});
